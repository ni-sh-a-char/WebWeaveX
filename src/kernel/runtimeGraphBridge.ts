/**
 * Converted from Python: core/kernel/runtime_graph_bridge.py
 * @generated — WebWeaveX python→javascript library port
 */

import { buildRuntimeGraph } from "../runtime_graph/runtimeGraphEngine.js";

export function mergeRuntimeGraph(irs: any): any {
  return buildRuntimeGraph(irs);
}
export { buildRuntimeGraph };
