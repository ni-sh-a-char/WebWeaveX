/**
 * Converted from Python: core/semantic/evidence_reconciliation_engine.py
 * @generated — WebWeaveX python→javascript library port
 */

import { reconcileEvidence } from "../evidence/reconciliationEvidenceEngine.js";

export function reconcileSemanticEvidence(claims: any): any {
  return reconcileEvidence(claims);
}
export { reconcileEvidence };
