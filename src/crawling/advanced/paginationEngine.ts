/**
 * Converted from Python: core/crawling/advanced/pagination_engine.py
 * @generated — the upstream Python source is syntactically
 * invalid; importing it raises SyntaxError in both runtimes.
 */
import * as py from "../../runtime/pyCompat.js";

throw py.err("SyntaxError", "closing parenthesis ']' does not match opening parenthesis '(' (<unknown>, line 5)");
