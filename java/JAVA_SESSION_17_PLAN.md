# JAVA_SESSION_17_PLAN

**Phase A — next family. Mission active: 69 / 128.**

## Selected target: `core.memory` family

Per the Phase-A clean-sweep order (memory → identity → connectors-live remainder → interaction →
auth → repository). Note: several memory primitives (`RuntimeMemory`, `MemoryQuery`,
`MemorySearch`) already exist from the foundation slices — Phase 1 must enumerate the manifest-API
memory functions and target only the **unproven, dependency-clean** set.

## Plan

1. Phase 0: rebuild reality (fetch, HEAD==origin/java, matrix, validator, ranking).
2. Phase 1: per-API relative-aware dependency proof for every unproven `core.memory` manifest API;
   classify CLEAN/BLOCKED. Watch for the self-referential-output pattern (S14 `run_live_runtime`)
   and the non-serializable-as-blob caveat.
3. Phase 3–10: port clean APIs → vectors → byte-exact tests → governance → certify → push.

### Expected gain

- **69 → ~73** (depends on unproven-clean memory API count).

### Substrate impact

- **No new substrate expected.** Reuse determinism/crypto/json + existing memory classes.

### Queue after memory (Phase-A order)

identity (3) → connectors-live remainder (3) → interaction (2) → auth (1) → repository (1).
≈ 10 more clean APIs → ~83/128. Then Phase B (bs4-decouple ~26) + Phase C (lxml extraction ~10)
need upstream-canon changes — to be hierarchised in `JAVA_BLOCKER_HIERARCHY.md`.

**Mission not complete — 69 / 128. Continue.**
