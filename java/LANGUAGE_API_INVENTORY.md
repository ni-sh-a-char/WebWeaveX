# LANGUAGE_API_INVENTORY

**Session-31 source-derived public-API inventories for all four language branches.** Reconstructed directly from source (not from docs/ledgers). Python is canonical.

## Summary

| Language | Public APIs | Entry point | Toolchain |
|---|---:|---|---|
| Python | 128 | `webweavex/__init__.py` `__all__` | CPython 3.11 |
| JavaScript | 128 | `src/index.ts` → `src/publicApi.ts` (generated from Python `__all__`) | Node 22 / tsup |
| Dart | 110 | `lib/webweavex.dart` re-exports | dart SDK (absent in env) |
| Java | 108 | `tools/gen_java_parity_matrix.JAVA_PROVEN` → `io.webweavex.*` | JDK / Maven |

All four share the same canonical API *names* (Python `__all__`); JS/Dart/Java map them to idiomatic symbols (camelCase / Dart libraries / `io.webweavex.*#method`). The cross-language name↔symbol map is `PARITY_MANIFEST.json` (`api`, `dart_symbol`, …) and `tools/gen_java_parity_matrix.JAVA_PROVEN`.

## Python (128 — canonical `__all__`)

Public functions, classes (`UniversalInput`, `RuntimeKernel`), constants (`version`, `__version__`), and package-level wrappers. Full list = the `API` column of `UNIFIED_PARITY_MATRIX.md`.

## JavaScript (128 — `publicApi.ts`, generated to mirror Python `__all__`)

Idiomatic camelCase exports (e.g. `extractWeb`, `runAutonomousExtraction`) + `version`/`__version__`/`VERSION` constants + `UniversalInput` class. Generated by `tools/convergence/build_public_api.py`. **S31: added `__version__`, fixed stale `version` → reached 128/128.**

## Dart (110 — `lib/webweavex.dart` library re-exports)

Re-exports of `lib/src/**` Dart libraries; `dart_symbol` per API in the manifest. 18 APIs unimplemented (13 non-portable extraction/platform cluster = Partial/Deferred; 5 portable-pending). See `FINAL_CONVERGENCE_LEDGER.md`.

## Java (108 — `io.webweavex.*`, `JAVA_PROVEN` registry)

108 byte-exact-certified public symbols (`pkg.Class#method`), each backed by golden vectors + a `CrossLanguageParity*Test`. 20 APIs formally blocked (verified S31). See `JAVA_FINAL_AUDIT.md`.

## Inventory categories (per mission Phase A)

- **Functions / runtime entry points**: the bulk of the 128 (extract/crawl/query_*/run_*/save_*/load_*…).
- **Classes**: `UniversalInput`, `RuntimeKernel` (+ Java/Dart/JS runtime classes).
- **Constants**: `version`, `__version__` (all four = "2.1.0").
- **Modules/packages**: Python `core/*`; JS `src/*`; Dart `lib/src/*`; Java `io.webweavex.*` (41 packages).
- **Interfaces/enums**: language-idiomatic (Java `PageView`/`StreamPage`/`ScrollPage`/`ReplayPage` duck-typed contracts; TS types `PipelineOptions`/`ExtractionEnvelope`); not part of the canonical 128 user surface.
