{
  "method": "Every {api, input} vector in validation/parity/*_api_vectors.json executed LIVE in each language; det_hash byte-compared. Python: cross_py.py (kwargs/args/adapters mirroring test/parity Dart shapes). JavaScript: cross_js.mjs (signature-driven positional + canonical engine-port overrides). Dart: test/parity suite asserts the identical committed hashes (1288 tests), which cross_py.py re-derives live from updated Python.",
  "vector_files": 20,
  "vectors": 201,
  "scenario_vectors_covered_by_dedicated_runners": 23,
  "results": {
    "python_live_vs_committed": {"match": 201, "mismatch": 0, "errors": 0},
    "javascript_live_vs_committed": {"match": 201, "mismatch": 0, "errors": 0},
    "dart_vs_committed": "test/parity suite (asserts same hashes) — see dart test run"
  },
  "real_divergences_found_and_fixed": [
    {
      "id": "JS-CALLKW-TIMELINE",
      "file": "src/reconstruction/runtimeReconstructionOrchestrator.ts",
      "bug": "generated py.callKw used wrong parameter-order list ['events','propagation'] for buildRuntimeTimeline(events, actions, mutations, synchronization, execution, recovery, replay, tick) — every argument dropped, timeline always empty",
      "fix": "direct positional call",
      "blast_radius": "run_reconstruction_runtime, run_reconstruction_for_extraction (6 vectors)"
    },
    {
      "id": "JS-CALLKW-MUTATIONS",
      "file": "src/synchronization/runtimeSyncOrchestrator.ts",
      "bug": "py.callKw order list ['prior','mutation'] for trackRuntimeMutations(changes, tick) — arguments dropped",
      "fix": "direct positional call",
      "blast_radius": "run_synchronized_runtime, run_sync_for_extraction (4 vectors)"
    },
    {
      "id": "JS-PYSOUP-FINDALL-TRUE",
      "file": "src/runtime/pyCompat.ts (PyTag.find_all)",
      "bug": "bs4 find_all(True) means 'match every tag'; PySoup stringified true -> matched tags named 'true' -> 0 results; semantic anchors never found from html",
      "fix": "name === true treated as match-all",
      "blast_radius": "heal_selector semantic_anchor strategy (vector 9), buildSemanticAnchor"
    },
    {
      "id": "FEFF-TRAILING-STRIP",
      "files": ["src/determinism/normalization.ts", "lib/src/determinism/normalization_core.dart"],
      "bug": "ECMAScript \\s includes U+FEFF (and misses U+001C-U+001F); Python \\s does not — trailing U+FEFF stripped in JS/Dart only",
      "fix": "explicit Python-\\s character class in both runtimes"
    },
    {
      "id": "DART-UTF8-BOM-STRIP",
      "files": ["lib/src/crypto/kaalka_runtime.dart", "lib/src/semantic/semantic_runtime.dart", "lib/src/runtime_memory_family/runtime_memory_persistence_engine.dart", "lib/src/persistence/fingerprint_hex.dart"],
      "bug": "dart:convert utf8.decode strips a leading BOM; Python/Node preserve it — decrypt roundtrip lost a leading U+FEFF (36 of 10000 vectors)",
      "fix": "utf8DecodeParity (BOM-preserving decode)"
    }
  ],
  "verdict": "PASS after fixes"
}
