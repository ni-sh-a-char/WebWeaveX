# Engineering archive

Historical validation, audit, and release reports for the `javascript` branch.

**Active parity harness:** [`validation/parity/`](../../validation/parity/) (not archived).

**Specification:** [`docs/architecture/CROSS_LANGUAGE_PARITY.md`](../architecture/CROSS_LANGUAGE_PARITY.md).

Regenerate release reports:

```bash
npm run reports:final
```
