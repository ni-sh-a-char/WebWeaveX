# Kaalka

Kaalka is the mandatory encryption layer for persisted runtime state.

```python
from core.crypto.kaalka_runtime_engine import encrypt_value, decrypt_value
```

See `KAALKA_CROSS_LANGUAGE_VALIDATION.md` for cross-language fixture protocol.
