# Extraction

Entry points: `extract`, `extract_web`, `extract_native`, `extract_repository`, `extract_document_runtime`.

All paths emit bounded, schema-locked output with optional phase flags (semantic, workflow, memory, execution, reconstruction).
