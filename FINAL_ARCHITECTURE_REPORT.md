# FINAL ARCHITECTURE REPORT

Single pipeline: `runCanonicalPipeline()` → `extractWeb` / text paths → unified graph.

Kaalka: external `kaalka` package (runtime v2). Replay: `validateReplayEquivalence`. Memory: federated merge with stable hashes.
